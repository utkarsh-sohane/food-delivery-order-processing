# Server
SERVER_PORT=8080

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=food_delivery
DB_SSLMODE=disable

# Logging (debug | info | warn | error)
LOG_LEVEL=info

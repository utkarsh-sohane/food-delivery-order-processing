# URL of the running Go backend
BACKEND_URL=http://localhost:8080

# Milliseconds between generated events (500 = ~2 events/sec)
EVENT_INTERVAL_MS=500

# Set to "true" to log the full JSON payload of each event
LOG_EVENTS=false
